Requirements
============

* A UNIX/Linux/Apple computer with a 64-bit architecture (e.g x86_64) with administrative
  rights

For unsupported architectures the docker image will have to be built from a Dockerfile.

